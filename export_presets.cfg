[application]
export_presets_version=3

[preset.0]
name="Windows Dev"
platform="Windows Desktop"
custom_template=false
binary="CELL.exe"
export_path="CELL.exe"
arch="x86_64"
export_mode="pck"

[preset.0.options]
runnable=true
run_in_debugger=false

# Keep default filters; do not exclude tests so dev builds include test scenes/scripts.
# Icon (if present) will be applied; set to your project icon if available.
icon="res://ASSETS/icons/cell_icon.ico"
